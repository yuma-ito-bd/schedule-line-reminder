{
  "name": "schedule-line-reminder",
  "version": "0.0.1",
  "module": "index.ts",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --profile private-admin",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "type": "module",
  "dependencies": {
    "@googleapis/calendar": "^9.7.9",
    "jose": "^5.9.6"
  }
}
